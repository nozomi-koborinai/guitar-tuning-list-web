name: guitar_tuning_app
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^0.14.0
  soundpool: ^1.0.1
  cupertino_icons: ^1.0.1
  webview_flutter: ^1.0.7
  lodash_dart: ^0.0.7
  js: ^0.6.2
  html: ^0.14.0+4

dependency_overrides:
  crypto: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    - images/